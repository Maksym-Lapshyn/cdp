1) Task1_DeletingScript.sql Don't declare temporary tables like @routesToDelete, put full select queries instead of  IN (SELECT * FROM @routesToDelete)
2) Task1_DeletingScript.sql what is BEGIN TRANSACTION second, without committing or rolling back?

Task 2 is ok.
Good job.
